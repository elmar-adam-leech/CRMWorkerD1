### Analyze Current State

Based on the recent changes implemented via the provided instructions (switch to Neon PostgreSQL, updates to db.ts for Neon driver, addition of seed.ts for initial user creation, schema push, and optional logging in auth-service.ts), the sign-in process is now functional for development. The repo structure remains standard: client/ for frontend, server/ for backend (including db.ts, auth-service.ts, routes.ts, scripts/seed.ts), shared/ for schema.ts, and configs like drizzle.config.ts. No sign-up process exists yet—only manual seeding for an initial admin user. Authentication uses bcrypt for hashing/comparison, with Drizzle ORM handling queries securely.

**Strengths**:
- Secure password storage with bcrypt (10 rounds, as per seed.ts).
- Multi-tenant foundations in schema (users have roles like super_admin).
- Neon switch improves scalability over D1, with proper connection via @neondatabase/serverless.

**Weaknesses**:
- No built-in sign-up: Relies on seed script, which is manual and not user-facing—potential for errors if not run.
- Generic errors in log-in (good for security, but no server-side rate limiting or brute-force protection).
- No MFA, password strength checks, or session management visible (assuming basic if any).

**Opportunities**:
- Add self-registration endpoint for simplicity.
- Enhance security with JWT sessions, rate limiting (e.g., express-rate-limit), and input sanitization.
- Improve UX with forgot-password flow using existing Gmail integration.

### Plan Modular Architecture

For auth, introduce modularity via a new DB table for auth_configs (e.g., enable_mfa, password_min_length per tenant). Abstract IAuthService in shared/ for future providers (e.g., OAuth-only). Keep sign-in simple but add /register for new users.

- Feature toggle: In future tenant_configs, but for now, add env flag for ENABLE_REGISTRATION.
- Interfaces: IAuthService with methods like register, login, resetPassword.
- Admin UI: Future settings for auth toggles; start with basic register form.
- User stories: As a new contractor, I can self-register to avoid manual seeding. As an admin, I can log in securely without brute-force risks.
- Architecture diagram (text-based):

```
Replit Shell -> npm run db:push (Neon schema) -> npm run seed (optional initial user)
Server -> db.ts (Neon) -> schema.ts (users + auth_configs table)
Client -> Register.tsx (new) / Login.tsx -> POST /register or /login -> routes.ts -> auth-service.ts (bcrypt + validation)
```

All instructions below are copy-paste friendly for Replit, atomic, and self-contained.

### Implement Incrementally

Recent changes appear properly implemented: Neon driver in db.ts prevents connection issues; seed.ts creates hashed users safely; schema push ensures tables exist. No major bugs noted, but we'll add improvements.

#### Security Concerns & Fixes
- **Concerns**: No rate limiting (vulnerable to brute force); no input validation (e.g., username length); passwords not enforced for complexity; seed script exposes default creds in code (change for prod); potential session hijacking if not using HTTPS/JWT.
- **Fix**: Add express-rate-limit for log-in attempts; validator for inputs; password policy in register.

#### Simplifications
- Replace manual seed with self-register endpoint—simpler for users, no shell commands needed.
- Use a single auth endpoint for log-in/register if possible, but keep separate for clarity.

#### Improvements
- Add registration flow.
- Enforce password rules (e.g., min 8 chars, mix case).
- Use JWT for sessions (instead of basic if any).
- Add basic rate limiting.

#### Step 1: Install Dependencies for Improvements
1. Open the Replit shell (bottom panel).
2. Paste and run this exact command:
   ```
   npm install express-rate-limit validator jsonwebtoken
   npm install -D @types/express-rate-limit @types/validator @types/jsonwebtoken
   ```
3. Expected: Packages added. Verify: Check package.json for new deps.

#### Step 2: Add auth_configs Table to Schema (for Future Modularity)
1. Open file explorer (left sidebar) → go to "shared" folder → click "schema.ts" to open it.
2. After the users table definition, paste this exact code block:
   ```typescript
   export const authConfigs = pgTable('auth_configs', {
     id: serial('id').primaryKey(),
     tenantId: integer('tenant_id').notNull(), // Link to tenants table if exists
     enableMfa: boolean('enable_mfa').default(false),
     passwordMinLength: integer('password_min_length').default(8),
     createdAt: timestamp('created_at').defaultNow(),
   });
   ```
3. Save (Ctrl+S).
4. Expected: Schema updated. No errors on save.

#### Step 3: Update drizzle.config.ts (If Needed for PG)
Already set for postgresql; no change.

#### Step 4: Push Updated Schema
1. Open the Replit shell (bottom panel).
2. Paste and run this exact command:
   ```
   npx drizzle-kit push
   ```
3. Expected: "Schema pushed successfully". If prompt, choose "No" for truncate.

#### Step 5: Add Register Endpoint & Improve Auth-Service
1. Open file explorer → go to "server" → click "auth-service.ts" to open it.
2. Replace/add to the file: Add register function with validation (assume existing login; extend it).
   ```typescript
   import { db } from './db';
   import { users, authConfigs } from '../../shared/schema';
   import { eq } from 'drizzle-orm';
   import bcrypt from 'bcrypt';
   import validator from 'validator';
   import jwt from 'jsonwebtoken';

   // Existing login...
   export async function login(username: string, password: string) {
     if (!validator.isAlphanumeric(username) || !validator.isLength(username, { min: 3, max: 20 })) {
       throw new Error('Invalid username');
     }
     const user = await db.select().from(users).where(eq(users.username, username)).get();
     if (!user || !(await bcrypt.compare(password, user.password))) {
       throw new Error('Invalid credentials');
     }
     const token = jwt.sign({ userId: user.id }, process.env.JWT_SECRET || 'secret', { expiresIn: '1h' });
     return { user, token };
   }

   export async function register(username: string, password: string, email: string) {
     if (!validator.isAlphanumeric(username) || !validator.isLength(username, { min: 3, max: 20 })) {
       throw new Error('Invalid username');
     }
     if (!validator.isEmail(email)) {
       throw new Error('Invalid email');
     }
     if (!validator.isStrongPassword(password, { minLength: 8, minLowercase: 1, minUppercase: 1, minNumbers: 1, minSymbols: 1 })) {
       throw new Error('Password too weak');
     }
     const existing = await db.select().from(users).where(eq(users.username, username)).get();
     if (existing) {
       throw new Error('User exists');
     }
     const hashed = await bcrypt.hash(password, 10);
     const [newUser] = await db.insert(users).values({
       username, password: hashed, email, role: 'admin', createdAt: new Date(),
     }).returning();
     return newUser;
   }
   ```
3. Save (Ctrl+S).
4. Expected: Functions added. Restart server to test.

#### Step 6: Add Rate Limiting & Register Route
1. Open file explorer → go to "server" → click "routes.ts" to open it (or app.ts if using Express app).
2. At top, add imports; then apply limiter to auth routes (assume Express app in routes.ts):
   ```typescript
   import rateLimit from 'express-rate-limit';
   import { login, register } from './auth-service';

   const authLimiter = rateLimit({
     windowMs: 15 * 60 * 1000, // 15 minutes
     max: 5, // limit each IP to 5 requests per windowMs
     message: 'Too many attempts, try again later',
   });

   app.post('/login', authLimiter, async (req, res) => {
     try {
       const { username, password } = req.body;
       const result = await login(username, password);
       res.json(result);
     } catch (error) {
       res.status(401).json({ error: (error as Error).message });
     }
   });

   app.post('/register', async (req, res) => {
     try {
       const { username, password, email } = req.body;
       const user = await register(username, password, email);
       res.json(user);
     } catch (error) {
       res.status(400).json({ error: (error as Error).message });
     }
   });
   ```
3. Save (Ctrl+S).
4. Expected: Routes updated. Add JWT_SECRET to Secrets: Tools → Secrets → Key: JWT_SECRET, Value: a-strong-secret.

#### Step 7: Add Register Form to Frontend
1. Open file explorer → go to "client" → right-click "pages" or "components" → New File → "Register.tsx".
2. Paste basic form (React + Tailwind, per design_guidelines.md):
   ```tsx
   import React, { useState } from 'react';

   const Register: React.FC = () => {
     const [username, setUsername] = useState('');
     const [password, setPassword] = useState('');
     const [email, setEmail] = useState('');
     const [error, setError] = useState('');

     const handleSubmit = async (e: React.FormEvent) => {
       e.preventDefault();
       try {
         const res = await fetch('/register', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ username, password, email }),
         });
         if (!res.ok) throw new Error(await res.text());
         // Redirect to login or dashboard
         window.location.href = '/login';
       } catch (err) {
         setError((err as Error).message);
       }
     };

     return (
       <form onSubmit={handleSubmit} className="max-w-md mx-auto p-4 bg-white rounded shadow">
         <input type="text" value={username} onChange={e => setUsername(e.target.value)} placeholder="Username" className="block w-full mb-2 p-2 border" />
         <input type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Password" className="block w-full mb-2 p-2 border" />
         <input type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="Email" className="block w-full mb-2 p-2 border" />
         {error && <p className="text-red-500">{error}</p>}
         <button type="submit" className="bg-blue-500 text-white p-2 rounded">Register</button>
       </form>
     );
   };

   export default Register;
   ```
3. Save (Ctrl+S).
4. Update router (e.g., App.tsx) to include <Route path="/register" component={Register} /> if using react-router.
5. Expected: New page at /register in preview.

### Test & Validate
- Unit tests: Extend auth-service.test.ts.
1. Open shell: `npx vitest server/auth-service.test.ts` (add tests for register, validation).
2. Expected: Pass with mocks for db.

- Edge cases: Weak password (fails), duplicate user (fails), brute force (limited after 5 tries).
- Manual: Preview → /register → create user → log in with new creds.

### Document & Deploy
Add to README.md (create if missing): "Auth: Use /register for new users. Security: Bcrypt hashing, rate limiting, validation. Run `npm run dev` for local."

For deploy:
1. Shell: `git add .` → `git commit -m "Improve auth with register and security"` → `git push`.
2. Replit Tools → Deployments → Create Autoscale deployment.
3. Expected: Hosted URL; test register/log-in there.